import { InstructorIntegrityComponent } from './instructor-integrity.component'

describe('InstructorIntegrityComponent', () => {
  it('should mount', () => {
    cy.mount(InstructorIntegrityComponent)
  })
})
