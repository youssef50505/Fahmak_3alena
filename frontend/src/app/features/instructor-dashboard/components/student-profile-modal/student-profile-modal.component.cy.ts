import { StudentProfileModalComponent } from './student-profile-modal.component'

describe('StudentProfileModalComponent', () => {
  it('should mount', () => {
    cy.mount(StudentProfileModalComponent)
  })
})